


function generatePascalTriangle(rows){
    let result=Array.from({length:rows},()=>[]);
    result[0].push(1);
    for(let i=0;i<rows;i++){
        for(let j=0;j<i+1;j++){
            if(j<)
        }
    }
}